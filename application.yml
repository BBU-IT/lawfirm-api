spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lawfirm_db   # ← service name, not container name
    username: postgres
    password: 123@456
  jpa:
    hibernate:
      ddl-auto: update   # or validate/create-drop depending on your needs