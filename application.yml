spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/lawfirm_db   # ← service name, not container name
    username: postgres
    password: Sup3rStr0ngP4ssw0rd!
  jpa:
    hibernate:
      ddl-auto: update   # or validate/create-drop depending on your needs